name: Build export-builder image

on:
  push:
    branches: [main]
    paths:
      - "services/export-builder/**"
      - "crates/bvault-export/**"
      - "crates/bvault-core/**"
      - "crates/bvault-meta/**"
      - "crates/bvault-store/**"
      - "Cargo.toml"
      - "Cargo.lock"

permissions:
  id-token: write
  contents: read

jobs:
  build:
    uses: ./.github/workflows/build-image.yml
    with:
      service: export-builder